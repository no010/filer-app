{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "filer default capability — window + dialog (folder picker) + autostart.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "autostart:default",
    "core:event:default"
  ]
}
